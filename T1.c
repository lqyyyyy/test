#include <stdio.h>

void process(int va[],int vb[])
{
   int i;
   int vc[i];
   do{
        vc[i]==va[i]-vb[i];
   }while (i>=0);
}

int main(void)
{
    int i,n;
    int a[n];
    int b[n];
    int c[n];

    printf("a[n]=");scanf("%d",a[n]);
    printf("b[n]=");scanf("%d",b[n]);

    for(i=0;i<=n;i++)
        process(a[i],b[i]);
        printf ("%d",&c[i]);
    return 0;
   }

